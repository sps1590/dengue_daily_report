# Required for the "Analyse data" brief and for the model extraction fallback.
# Without it the app still fetches, parses and exports Excel.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-sonnet-4-6.
ANTHROPIC_MODEL=claude-sonnet-4-6
